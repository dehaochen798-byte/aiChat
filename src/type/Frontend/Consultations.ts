export interface frontendConsultationSessionResponse {
  expiryTime: number;
  initialMessage: string;
  messageCount: number;
  sessionId: string;
  startTime: number;
  status: string;
  userHash: number;
}
